{
  "name": "@memecoinbot/backtest",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"lint:backtest ok\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@memecoinbot/indicators": "workspace:*",
    "@memecoinbot/paper-engine": "workspace:*",
    "@memecoinbot/risk": "workspace:*",
    "@memecoinbot/scoring": "workspace:*",
    "@memecoinbot/shared": "workspace:*",
    "@memecoinbot/strategies": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
