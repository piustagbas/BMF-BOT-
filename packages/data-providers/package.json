{
  "name": "@memecoinbot/data-providers",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"lint:data-providers ok\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@memecoinbot/indicators": "workspace:*",
    "@memecoinbot/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
